import { createSlice } from "@reduxjs/toolkit";

const initialState = {};
export const plannerSlice = createSlice({
  name: skkrt,
  initialState,
  reducers: {
    addTravels: (state, action) => {},
  },
});
